---
{
  "version": "0.5.3",
  "previous": "0.5.2",
  "title": "ambiente único: fluxo arquivo→arquivo, sem amarração a apps (Cowork)",
  "risk": "low",
  "autoApply": false,
  "affects": [
    "CLAUDE.md (seções 'Onde mora cada coisa' + 'Ciclo' → tabela de fluxo por agente)",
    "docs/agents/review-llm-fila-meta.md (novo: fila meta do Review.LLM)"
  ],
  "ops": []
}
---

## O que mudou na 0.5.3

O método deixa de amarrar os papéis a **apps** (chat = pensar / Cowork = agir /
Code = implementar) e passa a tratar tudo como **fluxo arquivo → arquivo no mesmo
ambiente**:

- A separação de papéis **já era** o que distinguia o trabalho — e ela vive nos
  **agentes** (`agente*.md`), não no app. Cada **execução de um agente é uma
  sessão**; o **handoff entre sessões é o arquivo de output**, não o contexto.
- O `CLAUDE.md` ganha uma **tabela de Ciclo** (agente → inputs → outputs) que
  substitui as antigas seções "Onde mora cada coisa" e "Ciclo" (que mapeavam
  estágio→app com Cowork/Claude Code).
- **Review.LLM** passa a **só ler** o `llm-report-inc{N}.md` (nunca anexa — evita
  auditar o próprio rastro). A **fila meta** ganha arquivo: `review-llm-fila-meta.md`
  (na pasta dos agentes); o Review.Product **anexa** itens roteados como "processo",
  o Review.LLM **classifica e mantém**.
- O `agente-kickoff` passa a **gravar o briefing na raiz** (`Kickoff.md`) — antes o
  caminho não estava cravado.

Arquivos de template (`pipeline.md`, `spec-guide.md`, `agents/*`, `lessons-learned.md`)
o diff de estado reconcilia sozinho. As lições antigas sobre "divisão entre
ferramentas" foram reescritas para "divisão entre agentes; handoff por arquivo;
ambiente único", e as notas exploratórias multi-app ficaram **marcadas como
superadas** (não apagadas — registro de decisão).

## Conduza com o humano (parte não-mecânica)

O `CLAUDE.md` é customizado → o `update` o marca como REVISAR e **não** o toca.
Ao reconciliar o handoff:

- **Substitua** as seções **"Onde mora cada coisa"** e **"Ciclo"** (a tabela
  estágio→ferramenta com Cowork/Claude Code e a lista de passos por app) pela
  **tabela de Ciclo (fluxo arquivo → arquivo)** da versão nova — uma linha por
  agente, com inputs/outputs. Veja a referência em `common/claude-md.template.md`.
- **Preserve** todo o resto do seu `CLAUDE.md` (stack, arquitetura, convenções,
  estado).
- Confira referências a `Cowork`/`Claude Code` como **distinção de etapa** no
  restante do seu `CLAUDE.md` e em scripts/CI do projeto — troque por
  estágio/agente. (Mencionar "Claude Code" como o ambiente é ok; usá-lo para
  **distinguir** um estágio dos outros não, já que tudo roda nele.)

Depois, confira:

- `docs/pipeline.md` e `docs/spec-guide.md` não distinguem mais por app.
- Se o Review.Product roteia processo, o item cai em `docs/agents/review-llm-fila-meta.md`.
